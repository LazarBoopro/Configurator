// Define the type for your values state
export interface ValuesType {
  pillars: {
    name: string;
    texture: string;
  };
  wall: {
    name: string;
    texture: string;
  };
  application: string;
  height: number;
  length: number;
}
